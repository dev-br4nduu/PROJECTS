# --- Core (obrigatório) ---
Flask==2.3.3
Flask-CORS==4.0.0
python-dotenv==1.0.0
anthropic==0.39.0
requests==2.31.0

# --- Aprendizado real: RAG + feedback + treino do classificador ---
# numpy + scikit-learn são tudo que o RAG (embeddings hashing) e o trainer
# de rede neural (numpy from scratch) precisam. Rodam em CPU, offline.
numpy>=1.24
scikit-learn>=1.3

# --- Opcionais ---
# Embeddings neurais (melhor que o fallback hashing). Requer download de modelo.
# sentence-transformers>=2.2
# Fine-tuning LoRA de LLM aberto (ver requirements-ml.txt) — requer GPU.
